name: risuai

services:
  risuai:
    container_name: risuai
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/yas-zoa/risuai:latest
    restart: always
    ports:
      - 6001:6001
    volumes:
      - risuai-save:/app/save

volumes:
  risuai-save:
